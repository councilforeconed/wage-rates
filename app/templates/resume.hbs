<h3>Résumé</h3>

<table>
  <tbody>
    <tr>
      <th>Name</th>
      <td>{{#if worker.name}}{{worker.name}}{{else}}xxx{{/if}}</td>
    </tr>
    <tr>
      <th>Math Ability</th>
      <td>{{#if worker.mathTestPoints}}{{worker.mathTestPoints}}{{else}}xxx{{/if}}</td>
    </tr>
    <tr>
      <th>Hand-Eye Coordination</th>
      <td>{{#if worker.handEyeTestPoints}}{{worker.handEyeTestPoints}}{{else}}xxx{{/if}}</td>
    </tr>
  </tbody>
</table>

{{#link-to 'worker' class="block-button"}}Go Back{{/link-to}}