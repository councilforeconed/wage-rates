import { test, moduleFor } from 'ember-qunit';

import Question from 'wagerates/models/math-question';

moduleFor('model:producer', "Unit - MathQuestionModel");

test('existence of class', function () {
  ok(Question);
});